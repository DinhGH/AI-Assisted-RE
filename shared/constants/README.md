# constants

<!--
  This folder contains shared constant values.
  It exists to keep cross-service identifiers and defaults centralized.
-->

## Purpose

Store shared constants and enum-like values.

## Responsibilities

- Avoid string duplication across modules
- Support consistent naming and default values
- Keep shared configuration discoverable

## Example usage

Place status labels, quality dimensions, and system-wide defaults here.
