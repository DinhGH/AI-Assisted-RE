# Shared

<!--
  This folder contains cross-service contracts and lightweight shared definitions.
  It exists to prevent duplicated constants and keep service boundaries explicit.
-->

## Purpose

Store shared types, interfaces, and constants used by multiple services.

## Responsibilities

- Define cross-service contracts
- Centralize shared enums and identifiers
- Keep reusable protocol shapes version-aware

## Example usage

Use these files as reference-only contracts or generated bindings, depending on the implementation strategy.
