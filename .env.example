# Root environment example for the monorepo.
# This file exists so each service can be configured consistently during setup.

# Shared settings
NODE_ENV=development
APP_NAME=ai-assisted-requirement-quality-analysis

