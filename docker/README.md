# Docker

<!--
  This folder contains container build files for each service.
  It exists to make local and CI execution consistent across environments.
-->

## Purpose

Define container images for the frontend, backend, and AI engine.

## Responsibilities

- Provide service-specific container build instructions
- Keep runtime dependencies isolated
- Support local orchestration via Docker Compose

## Example usage

Use these files as the source of truth for service image builds.
